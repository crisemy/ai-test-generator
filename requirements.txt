# Core dependencies for the AI Test Case Generator project
streamlit>=1.38.0          # Web UI
groq>=0.9.0                # Groq API oficcial Client
python-dotenv>=1.0.1       # Safe .venv handling

# Future additions for ML-based test generation:
scikit-learn>=1.5.0        # ML (RandomForest, etc.)
pandas>=2.2.0              # Datasets handling
numpy>=1.26.0,<2.0.0       # Common dependency pinned <2 for SHAP and Scikit-learn compatibility
shap>=0.40.0,<0.50.0       # ML Explainability pinned <0.50 to avoid needing numpy 2.0
matplotlib>=3.7.0          # Plotting engine for SHAP
